// Read this before running a Swing program: draw outlines, fill colors inside.
public class GraphicsMethods {
    // g.drawLine(x1, y1, x2, y2);
    // g.drawRect(x, y, width, height);
    // g.fillOval(x, y, width, height);
}
